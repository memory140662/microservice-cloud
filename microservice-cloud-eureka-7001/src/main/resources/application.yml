server:
  port: 7001
#eureka:
#  instance:
#    hostname: localhost
#  client:
#    register-with-eureka: false # 是否向註冊中心註冊自己
#    fetch-registry: false       # false 表示自己就是註冊中心，我得職責就是維護實例，並不需要去檢索服務
#    service-url:
#      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/ # 設置與 Eureka Server 交互的地址查詢服務和註冊服務都需要依賴這個地址